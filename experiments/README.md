# Experiments

Working experiment code, organized per paper and claim:

```
experiments/<paper-slug>/<claim-slug>/
```

This is where Phase 4 (implement) and Phase 5 (benchmark) code actually
lives; the corresponding `papers/<paper-slug>/claims/<NN>-*/README.md`
links back here and reports the results, rather than duplicating code
inline in documentation.

Empty as of Phase 0.
