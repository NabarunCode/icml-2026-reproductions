# Scripts

Small, reusable CLI entry points that don't belong to one specific
experiment: dataset download/sampling, environment-info capture, repo-wide
maintenance. Not a dumping ground for one-off experiment code — that
belongs under `experiments/`.

Empty as of Phase 0.
