# Benchmarks — Incremental BPE Tokenization

Phase 5 benchmark scripts and raw run output for this paper, following
the repository-wide standard in `docs/BENCHMARK_PROTOCOL.md` (warm-up,
>=10 repetitions, mean + variance, raw samples committed,
`environment.json` next to every run).

Raw output belongs here; the curated numbers that make it into a claim's
verdict belong in `../results/` and the claim's `README.md`.

Empty until Phase 5 begins.
