# Scripts — Incremental BPE Tokenization

Paper-specific CLI helpers (dataset download/sampling, vocab conversion,
figure regeneration). Reusable *cross-paper* tooling belongs in
`src/repro_core/` at the repo root instead — and only once a second
paper actually needs it.

Empty so far.
