# Results — Incremental BPE Tokenization

Curated, final result tables for this paper — the output of Phase 6
(verify claims). Every published result here carries:

- its `environment.json` (generated by `uv run repro-env`, see
  `docs/BENCHMARK_PROTOCOL.md` §5), and
- a completed `CHECKLIST-<run-name>.md` per
  `docs/REPRODUCIBILITY_CHECKLIST.md`.

Raw/intermediate run data belongs in `../benchmarks/`, not here.

Empty until Phase 5/6 produce real numbers. Nothing in this directory is
ever estimated or copied from the paper.
