# Supplementary material — Incremental BPE Tokenization

Supplementary material distributed with the paper (appendix-only PDFs,
released datasets, extra proofs), if/when obtained. The paper PDF itself
lives in `../paper/`.

Empty — the version in `../paper/` already includes the appendices we
rely on (A, E, F, G, J).
